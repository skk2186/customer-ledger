# 阶段五：Windows 桌面发布候选验收报告

## 验收结论

**PASS**。

本阶段在 develop 分支、阶段四验收修复后的基线提交上完成了 Windows 桌面发布实现、干净 Windows 隔离验收和断网验收。全部发布条件通过，创建唯一阶段五本地提交。

## 发布候选

- 发布版本：1.0.0rc1
- 程序形态：PyInstaller Windows onedir
- 程序入口：CustomerLedger.exe
- 本机候选目录：dist\CustomerLedger
- 候选目录大小：29.79 MiB
- 服务实现：Waitress + pywebview，仅监听 127.0.0.1
- 用户数据与程序资源分离：数据和运行状态在本机应用数据目录，备份和导出在用户文档目录
- 数据库初始化只执行版本化迁移，不使用 create_all 代替迁移

## 修复与实现内容

1. 增加桌面入口、Windows 命名互斥体、Waitress 本机服务、健康检查和启动失败保护。
2. 增加资源目录、用户数据目录、运行目录、备份目录和导出目录的明确解析规则。
3. 增加 PyInstaller onedir 规格和 Windows 构建脚本，构建脚本会检查模板、静态资源、迁移文件以及敏感运行文件。
4. 版本统一为 1.0.0rc1，补充 Windows 部署、用户启动、已知限制和最终验收说明。
5. 增加桌面启动、迁移失败保留原账库、单实例、回环服务和路径隔离测试。

## 自动化测试与命令

- 阶段五加入 8 项测试；全部测试结果为 84 passed。
- ruff check .：PASS，输出 All checks passed!。
- flask db upgrade：PASS。
- 第二次 flask db upgrade：PASS，重复执行安全。
- flask db check：PASS。
- Windows 构建脚本：PASS，构建产物审计通过。
- 旧账 Dry Run：PASS。仅使用已授权真实旧表，处理 2 个文件，源文件摘要保持不变，未执行导入。
- Git 与发布目录检查：PASS。未提交运行数据库、旧表、工作簿、日志、备份或运行数据。

## 本机真实候选程序验收

以下均使用合成客户、合成金额和合成备注，不写入报告具体业务数据：

- 健康检查：PASS。
- 新增客户、发货、超额初始收款、客户账目、未分配预收、追加收款和汇总：PASS。
- 客户级收款与发货分配口径、账目页脚合计口径：PASS。
- 三种 .xlsx 导出：PASS，文件在内存中校验为有效工作簿格式，未提交导出文件。
- WPS 实际打开：PASS，使用显式 WPS 表格程序打开三份工作簿；未修改 .xlsx 默认 Excel 关联。
- 手动备份、增加合成记录、恢复备份并核对恢复结果：PASS。
- 暂未付款快捷操作：PASS，账目中无收款记录。
- 保护标记：PASS，写入和导出均被阻止。
- 第二实例：PASS，被单实例保护拦截。
- 关闭后重启并读取同一账库：PASS。
- 复制程序目录、保留用户数据目录后重新启动：PASS。
- 候选程序通过限制 PATH 和清除外部 Python 环境变量的副本目录启动：PASS；这不是干净操作系统验收的替代品。
- Windows Sandbox 干净会话：PASS。无 Python PATH、断网配置、候选程序启动、记账、页脚口径、预收显示和三种工作簿导出均通过。
- Windows Sandbox 外部网络阻断：PASS。Networking=Disable 会话中外部请求失败，回环服务正常。

补充的真实窗口关闭验收通过：向本次候选程序自身窗口发送正常关闭消息后，进程自行退出，未留下候选程序进程。

## 阶段五验收修复与最终结果

- 修复原因：阶段五初始记录只完成了主机上的限制 PATH 验收，尚不足以证明发布包能在无 Python 的干净 Windows 和完全断网条件下运行。
- 修复内容：启用 Windows Sandbox，使用 Networking=Disable 和只读候选程序目录映射，在隔离会话中完成启动、记账、超额收款、预收、账目页脚和三种导出验收；补充了真实窗口正常关闭验证。
- 新增测试：阶段五自动化测试 8 项；隔离会话增加无 Python、断网、候选启动、业务流程、账目口径和三种工作簿的验收检查。
- 实际测试数量：全部 pytest 84 passed。
- 手工验收结果：候选程序启动、客户和发货流程、超额初始收款、未分配预收、追加收款、汇总、备份恢复、重启持久化、程序目录替换、暂未付款、第二实例、安全锁、三种工作簿及 WPS 打开均 PASS；Excel 默认打开方式保持不变。
- 浏览器控制组件在本机初始化时退出，因此本次真实候选程序流程以本机 HTTP、页面模板和生成文件校验为依据，未宣称完成 GUI 视觉手工验收。
- 未创建新分支、PR 或远程推送；阶段五提交仅在本报告确认通过后创建。

最终结论：PASS。不创建新分支、PR、远程推送或 GitHub Release；仅创建本地提交：

stage 5: windows release candidate
