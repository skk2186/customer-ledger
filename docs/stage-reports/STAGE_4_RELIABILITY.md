# 阶段四：备份恢复、可靠性、审计、易用性和整体验收

## 验收结论

**PASS（阶段四验收修复后）**

本阶段在 `develop` 分支完成，未创建新分支、Pull Request 或远程推送。没有实现阶段五、Windows 打包或其他未授权范围。

## 基线与范围

- 基线阶段三提交：`4c68926a0a11d59d29cd6202d69e7f75c5bef9ce`。
- 当前工作范围：本机 SQLite 在线备份、清单和校验、原子恢复与失败回滚、审计只读展示、导出保护、账目分页、用户提示和整体验收。
- 保持既有金额公式、整数分存储、SQLite 唯一真值、Excel 契约和旧账导入语义不变。

## 可靠性缺口与设计

阶段四开始时的缺口是：没有统一备份清单和恢复界面，导出前没有备份闸门，旧账备份实现未统一，恢复后没有会计恒等式校验，客户账目一次性加载全部发货，且缺少阶段四自动化验收覆盖。

新增 `backup_service.py` 统一使用 SQLite Online Backup API。备份写入临时文件后原子替换，清单使用独立 JSON 文件，包含格式版本、创建时间、原因、应用版本、结构版本、数据库文件名、文件大小和 SHA256，不记录客户、金额、备注或完整行。系统验证文件存在、大小、哈希、可打开性、`integrity_check` 和结构版本；只接受当前兼容结构，未来结构或损坏备份会阻止恢复。

备份触发点为每天第一次正常访问、旧账确认导入前、正式 Excel 导出前、恢复前，并支持页面手动备份。每日备份按本地日历日期去重；空账库策略为先完成版本化迁移后再备份，无法读取或不存在的账库会安全阻止操作。应用只清理配置 `BACKUP_DIR` 中可识别的旧有效应用备份，保留最近 30 份，不删除未知文件。

恢复页面只展示中文的时间、原因、大小和状态。恢复必须经过单独确认，确认文案明确说明当前账目会回到备份状态。恢复前先保存当前账库；临时文件校验通过后原子替换，恢复后重新连接并校验结构、完整性和会计恒等式，失败时自动回滚；自动回滚失败会明确阻止继续写入。

## 审计、错误和界面

复用现有 `AuditEvent`，增加备份、恢复和旧账确认导入的操作记录。审计页面只显示时间、对象类别和操作类别，不显示完整 JSON、金额快照、客户备注或敏感路径；没有删除入口。

导出、备份、恢复、旧账确认和全局错误均转换为普通用户可理解的中文提示，不展示 traceback、SQL、ORM 表示或绝对敏感路径。首页增加备份恢复入口，旧账迁移降为次要入口。客户账目按配置页大小稳定分页，宽表保留横向滚动。发货预览和账目余额使用“冲减”“预收/冲减”“已结清”等语义，派生负数仍保留在计算和数据库中。

所有模板和静态资源均为本地资源；已清理阶段四用户界面中的无意义英文并检查无 CDN/网络依赖。

## 测试与人工验收记录

- 原有阶段一至三测试：51 个通过。
- 阶段四可靠性/端到端专项：13 个通过。
- 本次恢复安全定向测试：11 个通过。
- 阶段四匿名性能专项：1 个通过。
- 最终全量 pytest：76 个通过。
- ruff：通过。
- `flask db upgrade`：连续两次通过且无新升级操作。
- `flask db check`：通过。
- 性能规模：100 个合成客户、10000 条合成发货、100 笔合成收款、1000 条合成分配；数据库准备 0.915 秒，客户列表 0.105 秒，单客户账目 0.082 秒，客户汇总 0.289 秒，全量 Excel 16.704 秒，备份 0.053 秒，完整性校验 0.011 秒，未使用真实客户或真实账务数据。
- 端到端流程：新增客户、发货、多笔收款、预收、分配、账目、汇总、三类 `.xlsx` 读回、手动备份、修改、恢复、审计均已自动化验证。
- 旧账流程：既有合成 `.xls` 试算、确认、失败不写入和幂等测试继续保留；网页确认使用应用备份目录。
- 手工本机页面验收：使用临时合成数据库启动 Flask，完成首页、客户、发货、超额收款、账目、未分配预收、追加收款、汇总、手动备份、修改、恢复二次确认、恢复验证和审计页面；服务正常关闭且端口无残留。浏览器连接组件因本机沙箱刷新异常退出，视觉操作改以本机 HTTP 页面、模板结构和 1024px/820px/580px 响应式 CSS 断点核验，125%/150% 缩放未使用真实客户数据操作。

## 21 项验收清单

1. [x] 分支和阶段三提交基线正确。
2. [x] 初始 pytest、ruff、迁移升级和数据库检查已执行。
3. [x] 统一 SQLite Online Backup API 服务。
4. [x] 每日首次打开和关键操作前备份触发。
5. [x] 空账库和备份失败安全策略。
6. [x] 稳定、不覆盖旧文件的备份命名。
7. [x] 非敏感备份清单和 SHA256。
8. [x] 文件、哈希、打开、完整性和结构版本校验。
9. [x] 最近 30 份有效应用备份保留策略。
10. [x] 备份列表与恢复二次确认页面。
11. [x] 原子恢复、恢复前备份和失败回滚。
12. [x] 恢复后结构、完整性和账务恒等式校验。
13. [x] 备份、恢复、导入和既有记账操作审计。
14. [x] 中文安全错误提示与敏感信息边界。
15. [x] 首页入口、负值语义、暂未付款和重复提交体验。
16. [x] 客户账目稳定分页与宽表横向滚动。
17. [x] 本地资源、无 CDN、离线检查。
18. [x] 匿名端到端流程和 Excel 读回。
19. [x] 100/10000 规模性能测试。
20. [x] 全量 pytest、ruff、重复迁移和 `db check`。
21. [x] git 状态、跟踪文件和运行产物检查。

## 风险与阶段五条件

当前备份恢复只接受与当前结构版本完全兼容的应用备份；旧结构备份需要后续明确的受控迁移方案，不能直接恢复。导出和全量汇总仍受本机磁盘、SQLite 单机并发和 Excel 工作簿大小影响。阶段五开始前必须完成本报告最终命令回填、真实部署副本上的备份恢复演练、权限检查和发布清单复核；不在本阶段实现打包或安装器。

## 阶段四恢复安全修复

1. 原缺陷：恢复替换失败后的自动回滚失败只返回提示，没有留下跨重启的真实写入保护状态。
2. 安全锁设计：恢复和回滚均失败时写入非敏感保护标记，只记录时间、原因代码和错误类别，不记录客户、金额、备注或账库内容。
3. 安全锁生命周期：应用启动及每个请求优先检查保护标记；标记存在时禁止所有状态变更请求，且没有普通用户自助解除入口，应用重启后仍然生效。
4. 检查顺序：安全锁检查早于每日备份和业务写入，锁定时首页、备份列表、操作记录、账目和汇总等安全只读页面仍可访问。
5. 恢复审计原子性：恢复后的完整性、结构版本、会计恒等式和“完成恢复”审计提交统一纳入最终确认；审计提交失败会回滚到恢复前状态。
6. 连接清理：恢复替换成功或失败回滚后均清理会话和连接池，后续查询只连接当前磁盘账库。
7. 双分页修复：发货和收款分别使用独立页码，翻动一侧保留另一侧页码；分配下拉框只加载当前发货页。
8. 破坏性恢复测试：新增覆盖恢复前备份失败、哈希不匹配、完整性失败、结构不兼容、原子替换失败、恢复后会计校验失败、恢复审计失败和回滚失败安全锁的测试，并验证锁下写接口无新增数据、重启后仍锁定。
9. 自动备份及外部文件测试：新增两个本地自然日各生成一次每日备份、同日不重复，以及任意外部清单不展示且不能通过恢复地址操作的测试。
10. 测试数量：最终收集并通过 76 个 pytest 测试，包含阶段一至四既有测试、本次恢复安全回归测试和性能专项。
11. 静态检查与迁移：ruff 通过；数据库迁移连续两次升级通过；数据库差异检查通过；未改变金额公式、整数分存储或 SQLite 唯一真值原则。
12. 手工验收与结论：使用合成数据完成新增客户、发货、超额收款、账目、预收、追加收款、汇总、备份、恢复确认及审计查看；验证保护状态下只读页面可用、写入被阻止、负值显示为普通会计可理解的中文，服务正常关闭且无残留进程。最终结论：**PASS**。
